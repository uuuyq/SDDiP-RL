exp01：相同的x_t-1，使用5个xi训练

exp02：i=1，所有阶段所有xi的数据进行训练

exp03：i=1，所有阶段所有xi的数据进行训练，设置ent_coef=0.01

exp04: i=1，所有阶段所有xi的数据进行训练，设置ent_coef=0.001，使用更新的policy网络（mask softmax） K=20

exp05: i=1，所有阶段所有xi的数据进行训练，设置ent_coef=0.0001，使用更新的policy网络（mask softmax），调整K=10